#include <iostream>
#include <vector>

using namespace std;

vector<int> V;

int f(int l, int i)
{
    if(k == l)
    {
        return 1;
    }
    for(int j = i)
}

int main()
{
    int N,M;
    cin >> N >> M;
    V.resize(N);
    for(int i = 0; i < N; i++)
    {
        cin >> V[i];
    }
    for(int k = 1; k <= 1; k++)
    {
        cout << f(0,k)%M << endl;
    }
    return 0;
}